import styled from "styled-components"

const Search = () => {
    return (
        <Wrapper>
            <h1>Search</h1>
        </Wrapper>
    );
}

export default Search;

const Wrapper = styled.div`

`;